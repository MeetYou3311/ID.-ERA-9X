name = "era9x-claims"
main = "worker.js"
compatibility_date = "2024-11-01"

# 仓库信息（计数写回这里）
[vars]
REPO_OWNER = "MeetYou3311"
REPO_NAME = "ID.-ERA-9X"
CLAIMS_PATH = "claims.json"

# GitHub Token（PAT，需对该仓库有 Contents: write）用以下命令设置，不会写进代码：
#   wrangler secret put GH_TOKEN
